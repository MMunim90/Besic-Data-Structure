#include<bits/stdc++.h>
using namespace std;
int main()
{
    // time complexity deppends on loop
    // space complexity depends on array

    int x; cin >> x;
    // int arr[x];  // space complexity O(x);

    // for(int i=0; i<x; i++)
    // {
    //     cin >> arr[i];
    // }

    // in 2D array

    int arr[x][x]; // space complexity O(x*x)
    return 0;
}