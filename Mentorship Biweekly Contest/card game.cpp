#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t; cin >> t;
    while (t--)
    {
        int n, x; cin >> n >> x;
        int sum = x;
        for(int i=n; i>0; i--)
        {
            if(i == x)
            {
                continue;
            }
            else if(sum % 2 == 0 && i%2 == 0)
            {
                sum +=
            }
        }
    }
    
    return 0;
}